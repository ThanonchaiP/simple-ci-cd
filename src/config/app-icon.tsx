import { ReactElement } from 'react';

import { CyberSecurityIcon } from '@/components/util-components';

export const APP_ICONS: Record<string, ReactElement> = {
  cyberSecurity: <CyberSecurityIcon />,
};

export type Module = {
  id: string;
  title: string;
  description: string;
  path: string;
};

export type ModuleItem = {
  id: string;
  title: string;
  description: string;
  appId: string;
  path: string;
};
